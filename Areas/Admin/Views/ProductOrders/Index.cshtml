@model IEnumerable<DentalShop.Areas.ViewModels.OrderUserViewModel>

@{
    ViewData["Title"] = "Index";
}


<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
             <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Email)
            </th>
            @*<th>
                @Html.DisplayNameFor(model => model.Count)
            </th>*@
           
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>


            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Email)
            </td>
            @*<td>
                @Html.DisplayFor(modelItem => item.Count)
            </td>*@
            <td>
                <a asp-area="Admin" asp-controller="ProductOrders" asp-action="Delete" asp-route-id="@item.Id" >DELETE</a> |
                <a asp-action="Details" asp-route-id="@item.Id">BAX</a> |
            </td>
        </tr>
}
    </tbody>
</table>
